This is a simple nano text editor that will be available on evey unix
machine - that is why I am showing it to you.

pwd: print working dir (where I am basically) cd: change directory (move around the file system)
1s: list out files and dirs mkdir: make a new dir (a.k.a.
"folder")
rm: remove files and folders (i.e. delete)
nano: a basic text editor

head: show the top lines of a file tail: show the bottom lines of a file cp: copy files or dirs
mv: move or rename a file or dir

-	Open an SSH client.

-	Locate your private key file. The key used to launch this instance is keyjz.pem


-	Run this command, if necessary, to ensure your key is not publicly viewable.
This is a simple nano text editor that will be available on evey unix
machine - that is why I am showing it to you.

pwd: print working dir (where I am basically) cd: change directory (move around the file system)
1s: list out files and dirs mkdir: make a new dir (a.k.a.
"folder")
rm: remove files and folders (i.e. delete)
nano: a basic text editor

head: show the top lines of a file tail: show the bottom lines of a file cp: copy files or dirs
mv: move or rename a file or dir

-	Open an SSH client.

-	Locate your private key file. The key used to launch this instance is keyjz.pem

-	Run this command, if necessary, to ensure your key is not publicly viewable.

chmod 400 ~/Desktop/BIMM 143/github/Class_16/keyjz.pem

-	Connect to your instance using its Public DNS:

ssh -i ~/~/Desktop/BIMM 143/github/Class_16/keyjz.pem ubuntu@ec2-44-252-77-168.us-west-2.compute.amazonaws.com

scp -i ~/Downloads/keyjz.pem -r ubuntu@ec2-44-252-77-168.us-west-2.compute.amazonaws.com:~/work .


